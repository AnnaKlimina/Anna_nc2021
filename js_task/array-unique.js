function unique(arr) {
  return Array.from(new Set(arr)); // создаем множество уникальных элементов, из которого делаем массив
}

// также это решение задачи https://learn.javascript.ru/task/array-unique-map
