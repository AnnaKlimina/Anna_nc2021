let scrollerWidth = elem.offsetWidth - elem.clientWidth;

// elem - элемент без рамок
// elem.offsetWidth - полная ширина элемента
// elem.clientWidth - ширина элемента без ширины прокрутки и рамок
